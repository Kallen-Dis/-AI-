# 智能AI旅游规划助手 · 使用教程

适合第一次打开网页、不知道从哪开始的使用者。

打开地址：http://127.0.0.1:5000  
页面右上角也有「教程」按钮，内容和下面一致。

---

## 这个系统是干什么的

你用普通话告诉旅行助手「小满」：去哪个城市、玩几天、大概花多少钱、喜欢什么。  
系统会调用 DeepSeek 做行程文案，调用百度地图搜景点、餐厅、酒店并画路线，结果显示在左侧文字和右侧地图上。

---

## 第一次使用（5 步）

### 1. 先点顶部快捷标签（推荐）

页面上方有三排标签：

- **天数**：1 / 2 / 3 / 5 / 7 天（单选，再点一次可取消）
- **偏好**：历史文化、美食、自然风光等（可多选）
- **预算**：1000 / 3000 / 5000 / 万元以上（单选）

这些会自动带进对话，不用再重复打字。

### 2. 和小满说话

1. 点击顶部输入框（「和小满聊聊你的旅行计划...」），对话面板会展开。
2. 用日常语言输入，例如：
   - `安庆 3 天，喜欢文化和美食，预算 2000`
   - `想去西安玩，带小孩，5 天 5000 块`
3. 回车或点纸飞机发送。

**必须说清目的地城市。** 天数、预算、偏好如果没选、也没说，小满会追问。信息齐了会自动开始规划，中间有加载动画，一般需要几十秒。

对话面板右上角的刷新按钮：清空当前对话，重新开始。

### 3. 看规划结果

规划成功后：

| 位置 | 你会看到 |
|------|----------|
| 左侧「行程」 | 文字日程、途经点、天气、图层开关 |
| 右侧地图 | 景点（蓝）、餐厅（橙）、酒店（绿）、路线 |
| 左侧「地点」 | 可筛选的地点列表，点击可定位到地图 |
| 右上角「游记」 | 按天生成的游记提纲 |

点地图上的标记，会弹出地址、评分等信息。

### 4. 自己改路线

- 途经点可以**拖拽改顺序**。
- 点途经点旁的「+」，搜索地名再添加。
- 在地图上**右键**，输入名称，可快速加一个点。
- 选择「驾车 / 步行 / 骑行 / 公交」，再点「规划路线」。
- 多天行程可点「第 1 天」「第 2 天」切换当天的点。
- 「地图图层」可单独关掉某类标记或路线。
- 侧栏右侧小箭头可以收起侧栏，地图更大。

### 5. 写游记并导出

切到「游记」页：

- 规划后会自动生成每天的条目（时间、标题、正文可改）。
- 「添加」：自己补一条。
- 「导出」：下载 Markdown 文件，方便保存。

---

## 示例对话

可以这样说：

```
我想去安庆玩 3 天，预算 2000 元，喜欢黄梅戏、古建筑和当地小吃。
```

或先点「3天 + 历史文化 + 美食 + ¥3000」，再只发一句：

```
目的地安庆
```

---

## 常见问题

**地图是白的、没有底图**  
到百度开放平台，给「浏览器端」Key 加上 Referer 白名单：  
`http://127.0.0.1:5000/*` 和 `http://localhost:5000/*`。

**一直转圈或提示规划失败**  
检查项目根目录 `.env` 里的 `DEEPSEEK_API_KEY` 是否有效、账户是否有余额；网络是否能访问 DeepSeek。可以换个说法再试。

**搜不到景点或路线是直线**  
检查 `KEY.md` / `.env` 里的百度服务端 Key，并确认已开通地点检索、地理编码、路线规划、天气等服务。

**想重新规划**  
点对话面板的刷新，或直接再说一个新城市、新天数。

**教程每次打开都弹出来**  
在教程窗口勾选「下次不再自动弹出」，再点「我知道了」。以后仍可点右上角「教程」打开。

---

## 给部署的人（可选）

仓库不含 `.venv` 和密钥。在项目根目录：

1. `python -m venv .venv`
2. `.\.venv\Scripts\pip.exe install -r MCP_map\requirements.txt`（macOS/Linux 用 `.venv/bin/pip`）
3. 复制 `.env.example` 为 `.env`，填入 DeepSeek 与百度 Key（格式也可参考 `KEY.md.example`）
4. 启动：

```text
cd MCP_map
..\.venv\Scripts\python.exe -c "from app import app; app.run(debug=False, host='127.0.0.1', port=5000)"
```

5. 浏览器打开 http://127.0.0.1:5000
